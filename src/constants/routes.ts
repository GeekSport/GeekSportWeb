/**
 * App routes
 * @file App 路由
 * @module app/routes
 * @author Jed
 */

export enum HomeRoutes {
  Home = 'Home',
  Square = 'Square',
  Find = 'Find',
  Personal= 'Personal'
}

export enum GuestbookRoutes {
  Guestbook = 'Guestbook',
}

export enum AboutRoutes {
  About = 'About',
  Github = 'Github',
  Setting = 'Setting',
  OpenSource = 'OpenSource'
}
